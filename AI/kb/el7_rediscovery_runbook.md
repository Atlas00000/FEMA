# EL7 Re-Discovery runbook (IS-EL7-02)

**Dry-run once:** `cd AI; python -m fema_ops el7-dry-run`  
**Apply lineage retire stamp (human only):** `python -m fema_ops el7-dry-run --apply`  
**Local two-terminal automation:** [`../../automated_edge_rediscovery_pipeline.md`](../../automated_edge_rediscovery_pipeline.md) (`AER-P0`…`P6`)  
**Hybrid dual-lane:** [`../../doc/dual_lane_rediscovery_pipeline.md`](../../doc/dual_lane_rediscovery_pipeline.md) — **`DLR-P3` complete**  
**Policy:** [`dlr_policy.json`](dlr_policy.json) · advisor [`../../ops/tester_queue/el7_policy.ps1`](../../ops/tester_queue/el7_policy.ps1) · roster [`challenger_roster.md`](challenger_roster.md)

## Dual-lane EL7 policy (DLR-P3-01)

| Rule | Value |
| ---- | ----- |
| Overnight / first wave default | **Lane A only** (`el7_enqueue.ps1`) |
| Escalate Lane B | After **≥2** Lane A non-promotes (reject/alternate) in lookback |
| Lane B cap / cycle | **1×** preferred (max 2 queued B hard cap) |
| Preferred B parent | `P1-BASELINE` (else first eligible roster parent) |
| Who enqueues B | **Human** — `enqueue_lane_b.ps1` (advisor never auto-enqueues B) |
| Promote | **Human AER-P6 only** — `decision.ps1` checklist; never swaps Terminal A |

```powershell
# Read-only advice (writes AI/data/live/dlr_el7_policy_latest.json)
powershell -File ops\tester_queue\el7_policy.ps1

# Default wave = Lane A
powershell -File ops\tester_queue\el7_enqueue.ps1 -Force -Max 3
powershell -File ops\tester_queue\drain.ps1 -Max 3

# Only when policy recommend_b=true (or operator hunt):
powershell -File ops\tester_queue\enqueue_lane_b.ps1 -Preset Challenger_P1BASE_adx_01 -Parent P1-BASELINE -Subsystem adx
```

## Steps

1. **Confirm trigger** — table in [`el7_trigger_table.md`](el7_trigger_table.md); check Observatory + health persistence.
2. **EL7-002 Snapshot** — set `lineage.json` `active_lock.retired_run_id` = current lock `run_id` (`--apply` or edit by hand). Parent remains PRODUCTION until a new lock promotes.
3. **EL7-003 Open EL1** — same canonical window as lock (`2026.01.01`–`2026.07.31`). Run `el7_policy.ps1`, then AER enqueue on Terminal B (**Lane A** = PRODUCTION one-axis clones):
   ```powershell
   powershell -File ops\tester_queue\el7_enqueue.ps1
   powershell -File ops\tester_queue\el7_enqueue.ps1 -Force -Max 3
   powershell -File ops\tester_queue\drain.ps1 -Max 3
   ```
   Manual factory path (same cap ≤3):
   ```powershell
   python -m fema_ops recommend
   python -m fema_ops factory --apply --index 0   # or --subsystem adx
   powershell -File ops\tester_queue\enqueue.ps1 -Preset <id>
   ```
   If advisor `recommend_b=true`, add **1×** Lane B from roster (not overnight-default).
4. **EL7-004 Validate** — `scorecard.ps1` + `gate-check` + [`../templates/promotion_checklist.md`](../templates/promotion_checklist.md).  
   Record with `ops/tester_queue/decision.ps1` (refuses Promote if G1 fail). Human only ([`raci.md`](raci.md)).
5. **EL7-005 No promote** — keep last acceptable lock; clear panic; do not auto-wire pause.

## Dry-run record

Latest plan artifact: [`el7_dry_run_latest.json`](el7_dry_run_latest.json) (regenerated by CLI).  
Policy advice: `AI/data/live/dlr_el7_policy_latest.json`.

## Forbidden

- Auto-promote PRODUCTION
- Live EMA/TP/SL/lot from AI
- >3 Lane A candidates in first wave
- Overnight-default Lane B without human intent
- Treating Tester collect as demo health
- Running Discovery Optimizer on Terminal A while a demo basket is open
